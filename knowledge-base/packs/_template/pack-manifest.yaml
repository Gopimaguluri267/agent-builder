pack_id: REPLACE_ME               # kebab-case, e.g. finance-kyc
pack_name: "REPLACE_ME — human-readable name"
version: "0.1.0"
extends: core
domain: REPLACE_ME                 # e.g. finance, healthcare
sub_domain: REPLACE_ME              # e.g. kyc, prior-auth

governance_floors:
  risk_tier: REPLACE_ME             # Low / Medium / High — see core/risk-tiers.md; set honestly, don't default to High just to be safe if the domain doesn't warrant it
  data_classification: REPLACE_ME   # Public / Internal / Confidential / Restricted

jurisdictions_supported:
  - REPLACE_ME                      # only list jurisdictions your regime-rules.md actually has researched content for

files:
  regime_rules: regime-rules.md
  controls_catalog_md: controls-catalog.md
  controls_catalog_json: controls-catalog.json
  allowlist_md: allowlist.md
  allowlist_json: allowlist.json

description: >
  REPLACE_ME — what this agent does, who uses it, and what it is explicitly
  barred from doing autonomously. Model this on finance-aml/pack-manifest.yaml's
  description.

open_gaps:
  - "REPLACE_ME — be explicit about what this pack does NOT yet cover (e.g. a jurisdiction allowed by core/governance-schema.md but not researched here). Don't leave this list empty unless you're certain there are no gaps — that's rarely true for a first pass."
