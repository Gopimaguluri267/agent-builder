# Usecase Template (placeholder)

Once this vertical's own layer (`regulatory-landscape.md`, `technical-standards.md`, `model-catalog.md`, etc.) is built out, copy `../../../banking-and-capital-markets/usecases/_template/` here and adapt it — the usecase-level bootstrap pattern (manifest, thin rules referencing the vertical layer by ID, controls catalog, allowlist) is identical across verticals; only the specific vertical-layer files being referenced differ.
